---
interface Props {
  fileName: string;
  alt: string;
}

const { fileName, alt } = Astro.props;
const basePath = "/assets/";
---

<img
  src={`${basePath}${fileName}`}
  alt={alt}
  style="max-width: 350px; height: 400px; object-fit: cover; border-radius: 50px;"
/>
